spring:
  application:
    name: book-service
server:
  port: 8100
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    service-url:
      default-zone: http://${eureka.instance.hostname}:8761/eureka/